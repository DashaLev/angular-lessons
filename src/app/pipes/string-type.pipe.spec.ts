import { StringTypePipe } from './string-type.pipe';

describe('StringTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StringTypePipe();
    expect(pipe).toBeTruthy();
  });
});
